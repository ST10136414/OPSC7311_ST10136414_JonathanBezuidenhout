<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners
        android:radius="8dp" /> <!-- Adjust the radius as needed -->
    <solid
        android:color="@android:color/black" /> <!-- Adjust the color as needed -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" /> <!-- Adjust the border color/width -->
</shape>